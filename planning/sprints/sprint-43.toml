sprint = "Sprint 43"
milestone = "Sprint 43"
milestone_description = "Actionable notification controls from direct messages"
base_branch = "main"
status_file = "planning/STATUS.md"

[[items]]
id = "S43-001"
title = "Unify open-auction deep links in direct notifications"
description = "Ensure every auction-related direct notification includes a reliable button to open the relevant auction post when possible."
priority = "P1"
estimate = "M"
tech_debt = false
labels = ["type:feature", "area:bot", "area:auctions", "priority:p1"]
assignees = []
create_draft_pr = true
acceptance = [
  "Outbid, buyout, finish, and moderation notices include open-auction CTA",
  "Link builder supports public usernames and internal /c links",
  "Fallback text path remains stable when link cannot be derived",
]

[[items]]
id = "S43-002"
title = "Add quick action to mute notification type from message"
description = "Allow users to disable the current notification category directly from the incoming direct message."
priority = "P1"
estimate = "M"
tech_debt = false
labels = ["type:feature", "area:bot", "area:ux", "priority:p1"]
assignees = []
create_draft_pr = true
acceptance = [
  "Notification payload includes disable-this-type action",
  "Action updates user preferences immediately",
  "User receives confirmation and way to re-enable in settings",
]

[[items]]
id = "S43-003"
title = "Add auction-level snooze controls for noisy lots"
description = "Introduce temporary per-auction snooze from notifications, with explicit expiry and visibility in settings."
priority = "P1"
estimate = "L"
tech_debt = false
labels = ["type:feature", "area:bot", "area:auctions", "priority:p1"]
assignees = []
create_draft_pr = true
acceptance = [
  "Users can snooze notifications for a specific auction for a fixed period",
  "Snooze state expires automatically",
  "Snooze status is visible and removable from settings",
]

[[items]]
id = "S43-004"
title = "Add direct notification control panel"
description = "Provide a compact control panel in private settings for active mutes and snoozes with one-tap revert actions."
priority = "P2"
estimate = "M"
tech_debt = false
labels = ["type:feature", "area:bot", "area:ux", "priority:p2"]
assignees = []
create_draft_pr = true
acceptance = [
  "Settings show active per-type mutes and per-auction snoozes",
  "Users can remove mutes and snoozes without commands",
  "UI works with private topics routing enabled",
]

[[items]]
id = "S43-005"
title = "Stabilize callback payload compatibility for notification actions"
description = "Harden callback parsing and backward compatibility for old notification messages and expired payloads."
priority = "P2"
estimate = "S"
tech_debt = true
labels = ["type:chore", "area:bot", "priority:p2", "tech-debt"]
assignees = []
create_draft_pr = true
acceptance = [
  "Unknown and stale payloads fail gracefully",
  "Old notification messages do not cause tracebacks",
  "Callback handlers return clear user-facing alerts",
]
