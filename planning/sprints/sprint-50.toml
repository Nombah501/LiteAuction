sprint = "Sprint 50"
milestone = "Sprint 50"
milestone_description = "Notification metrics triage speed and signal clarity"
base_branch = "main"
status_file = "planning/STATUS.md"

[[items]]
id = "S50-001"
title = "Add suppression-share percentages to /notifstats top sections"
description = "Show share percentages next to top suppression reasons for 24h, 7d, and all-time windows so operators can estimate concentration without manual math."
priority = "P1"
estimate = "M"
tech_debt = true
labels = ["type:enhancement", "area:ops", "area:metrics", "priority:p1", "tech-debt"]
assignees = ["Nombah501"]
create_draft_pr = true
acceptance = [
  "Top suppression sections include count and share for each row",
  "Share is computed against suppressed totals in matching window and handles zero-total windows safely",
  "Unit tests cover share formatting for regular and empty snapshots",
]

[[items]]
id = "S50-002"
title = "Add /notifstats window selector for focused triage output"
description = "Introduce a window selector argument (24h/7d/all) to keep output concise during active incidents and reduce operator scrolling."
priority = "P2"
estimate = "S"
tech_debt = true
labels = ["type:enhancement", "area:ops", "area:bot", "priority:p2", "tech-debt"]
assignees = []
create_draft_pr = true
acceptance = [
  "Command accepts window selector argument with clear validation errors",
  "Selected window highlights relevant totals and top suppression reasons only",
  "Existing compact mode remains backward compatible",
]

[[items]]
id = "S50-003"
title = "Document suppression-share triage heuristics for operators"
description = "Update runbook with practical thresholds and decision hints based on top suppression reason shares to standardize incident escalation quality."
priority = "P2"
estimate = "S"
tech_debt = true
labels = ["type:docs", "area:ops", "area:metrics", "priority:p2", "tech-debt"]
assignees = []
create_draft_pr = true
acceptance = [
  "Runbook adds share-based interpretation examples for top suppression reasons",
  "Escalation templates include share fields alongside absolute counts",
  "Examples map directly to `/notifstats` output terminology",
]

[[items]]
id = "S50-004"
title = "Redesign admin dashboard information hierarchy and visual language"
description = "Rework admin `/` into role-focused sections with priority metrics, status badges, quick actions, and progressive disclosure so operators can triage faster without losing data visibility."
priority = "P1"
estimate = "L"
tech_debt = true
labels = ["type:enhancement", "area:web", "area:admin", "priority:p1", "tech-debt"]
assignees = []
create_draft_pr = true
acceptance = [
  "Dashboard top area shows operational priority metrics and status chips before secondary analytics",
  "Secondary metric groups are still accessible but moved into structured sections with clearer labels",
  "Quick action navigation is presented as cards/buttons instead of a plain link list",
]

[[items]]
id = "S50-005"
title = "Unify admin list-page UX for moderation queues"
description = "Apply a shared list-page layout for complaints, fraud signals, auctions, trade feedback, violators, and appeals with consistent headers, filter bars, and table ergonomics."
priority = "P1"
estimate = "L"
tech_debt = true
labels = ["type:enhancement", "area:web", "area:moderation", "priority:p1", "tech-debt"]
assignees = []
create_draft_pr = true
acceptance = [
  "Key moderation list pages share a common page header and filter-toolbar visual system",
  "Tables keep existing data and actions while improving readability and scan speed on desktop and mobile",
  "Existing URL query parameters and pagination behavior remain backward compatible",
]

[[items]]
id = "S50-006"
title = "Refresh manage-user pages with structured operator workflows"
description = "Reorganize `/manage/users` and `/manage/user/{id}` into clear workflow sections (risk, moderation, rewards, reputation) with denser but readable cards and safer action grouping."
priority = "P1"
estimate = "M"
tech_debt = true
labels = ["type:enhancement", "area:web", "area:users", "priority:p1", "tech-debt"]
assignees = []
create_draft_pr = true
acceptance = [
  "Manage-users list has clearer search/action framing and improved row readability",
  "Manage-user detail page groups metrics and actions by workflow intent instead of a single metric stream",
  "Rewards, trade feedback, complaints, and fraud subsections remain fully functional with current actions",
]
