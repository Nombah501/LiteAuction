sprint = "Sprint 44"
milestone = "Sprint 44"
milestone_description = "Anti-noise controls for high-frequency notifications"
base_branch = "main"
status_file = "planning/STATUS.md"

[[items]]
id = "S44-001"
title = "Implement per-auction debounce for outbid notifications"
description = "Reduce repeated outbid direct messages by applying configurable debounce windows per user and auction."
priority = "P1"
estimate = "M"
tech_debt = false
labels = ["type:feature", "area:bot", "area:auctions", "priority:p1"]
assignees = []
create_draft_pr = true
acceptance = [
  "Outbid notifications are throttled by user and auction key",
  "Configurable debounce window is respected",
  "Critical events still bypass debounce where required",
]

[[items]]
id = "S44-002"
title = "Add notification batching and digest mode"
description = "Aggregate repetitive events into short digest messages to improve readability and reduce spam."
priority = "P1"
estimate = "L"
tech_debt = false
labels = ["type:feature", "area:bot", "area:ux", "priority:p1"]
assignees = []
create_draft_pr = true
acceptance = [
  "System can group multiple events into one digest message",
  "Digest windows are configurable",
  "Digest content includes actionable links where possible",
]

[[items]]
id = "S44-003"
title = "Introduce quiet hours for non-critical notifications"
description = "Allow users to configure quiet hours where non-critical notifications are delayed or muted."
priority = "P2"
estimate = "L"
tech_debt = false
labels = ["type:feature", "area:bot", "area:ux", "priority:p2"]
assignees = []
create_draft_pr = true
acceptance = [
  "Users can set quiet hours in settings",
  "Non-critical events are deferred during quiet hours",
  "Critical notifications are still delivered immediately",
]

[[items]]
id = "S44-004"
title = "Add notification priority model and delivery rules"
description = "Define priority tiers for event types and enforce tier-aware delivery behavior across debounce, digest, and quiet hours."
priority = "P1"
estimate = "M"
tech_debt = true
labels = ["type:chore", "area:service", "priority:p1", "tech-debt"]
assignees = []
create_draft_pr = true
acceptance = [
  "Event types are mapped to priority tiers",
  "Delivery policy is tier-aware and test-covered",
  "Policy changes are documented for maintainers",
]

[[items]]
id = "S44-005"
title = "Track anti-noise effectiveness metrics"
description = "Emit counters for sent, suppressed, and aggregated notifications to evaluate anti-noise impact."
priority = "P2"
estimate = "S"
tech_debt = false
labels = ["type:chore", "area:metrics", "priority:p2"]
assignees = []
create_draft_pr = true
acceptance = [
  "Counters are recorded for sent, suppressed, and aggregated events",
  "Logs include reason codes for suppression",
  "Metrics can be inspected from bot logs without extra tooling",
]
